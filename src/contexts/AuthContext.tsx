import React, { createContext } from "react"


export const AuthContext = createContext({}) //criado o contexto que será passado para o elemento filho


export function AuthContextProvider() {


    return (
    <></>
    )
}